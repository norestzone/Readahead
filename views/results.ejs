<h1>Search Results</h1>
<hr>
<% books.forEach(book => { %>
    <img src="<%= book.image %>"> 
    <h3><a href="/book/<%= book.isbn %> "><%= book.title %></h3></a> 
    <h4>Author(s): <%= book.authors %> | ISBN: <%= book.isbn %></h4>
<% }) %>